import * as react from "react-dom";

function NavSetting() {
    <button
      class="setting.btn"
      onClick={
        "setting()"
      }
    ></button>
}

function NavBtnBack() {
    // make that part later
}

export default function MyApp() {
    return (
      <div>
        <h1>Welcome to my app</h1>
        <MyButton />
      </div>
    );
}